#!/bin/bash
echo "${bold}sort${norm}"
echo "The sort command can be used to sort a large number of lines to the user's liking, there are several different options but for this assignment, you will only need default behavior."
echo "sort is also almost always piped into with a file."