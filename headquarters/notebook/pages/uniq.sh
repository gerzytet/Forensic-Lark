#!/bin/bash
echo "${bold}uniq${norm}"
echo "uniq is quite an interesting command that allows the user to see all lines in a file except for the ones that are identical, this is quite useful to people who need to delete a large amount of duplicated lines."
echo "uniq is typically piped into but can be used by itself."